name: grab fw_MintOS

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-24.04

    strategy:
      matrix:
        build_type: [decrypted]
      fail-fast: false

    steps:
      - name: Checkout repo
        uses: actions/checkout@v7
        with:
          repository: jmengelsk/BananaOS
          ref: MintOS
          submodules: 'recursive'

      - name: Integrating SSH Keys f. SF
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SF_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan frs.sourceforge.net >> ~/.ssh/known_hosts
      - name: Free disk space (1/3)
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache/CodeQL
          sudo docker image prune --all --force
          sudo docker builder prune -a

      - name: Free disk space (2/3)
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: true

      - name: Free disk space (3/3)
        uses: rokibhasansagar/slimhub_actions@main
        with:
          retain: 'compiler_cmake'

      - name: Set up build environment
        run: |
          sudo apt update
          DEBIAN_FRONTEND=noninteractive sudo apt install -yq \
            7zip attr ccache clang ffmpeg \
            libbrotli-dev libgtest-dev libprotobuf-dev libunwind-dev libpcre2-dev \
            libzstd-dev linux-modules-extra-$(uname -r) lld protobuf-compiler webp
          sudo modprobe erofs f2fs
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          echo -n "${{ secrets.PLATFORM_KEY_PK8 }}" | base64 --decode > security/unica_platform.pk8
          echo -n "${{ secrets.PLATFORM_KEY_PEM }}" | base64 --decode > security/unica_platform.x509.pem

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '21'
          cache: 'gradle'

      - name: Build dependencies
        run: |
          source ./buildenv.sh m51
          ./scripts/build_dependencies.sh

      - name: Download firmwares
        run: |
          source ./buildenv.sh m51
          mkdir -p ./out/odin/SM-S916B_EUX/
          mkdir -p ./out/odin/SM-M515F_BVO/
          ./out/tools/venv/bin/samloader -m SM-S916B -r EUX -i 35563097005209 download -v S916BXXS9EZCI/S916BOXM9EZCI/S916BXXS9EZCI/S916BXXS9EZCI -O ./out/odin/SM-S916B_EUX/
          ./out/tools/venv/bin/samloader -m SM-M515F -r BVO -i 353738482076286 download -v M515FXXS6DXE4/M515FOWO6DXE4/M515FXXS6DXE4/M515FXXS6DXE4 -O ./out/odin/SM-M515F_BVO/

      - name: Create odin Tarballs
        run: |
          tar -cvzf ./out/odin_SM-S901U1_AIO_S901U1UESAGZF3.tar.gz ./out/odin/SM-S901U1_AIO/
          tar -cvzf ./out/odin_SM-M515F_BVO_M515FXXS6DXE4.tar.gz ./out/odin/SM-M515F_BVO/

      - name: Extract firmwares
        run: |
          source ./buildenv.sh m51
          ./scripts/extract_fw.sh


      - name: Create fw Tarballs
        run: |
          tar -cvzf ./out/fw_SM-S916B_EUX_S916BXXS9EZCI.tar.gz ./out/fw/SM-S916B_EUX/
          tar -cvzf ./out/fw_SM-M515F_BVO_M515FXXS6DXE4.tar.gz ./out/fw/SM-M515F_BVO/

      - name: Uploading  Tarballs to SourceForge
        env:
          SF_LAB_DIR: ${{ secrets.SF_LAB_DIR }}
          
        run: |
          for BALL in out/*.gz; do
            echo "Uploading $BALL to SourceForge..."
            scp -i ~/.ssh/id_rsa $BALL $SF_LAB_DIR/           
          done
